[project]
name = "spotify-playlist-sorter"
version = "0.1.0"
description = "Sort your Spotify Liked Songs into per-genre and per-decade playlists, automatically."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Raphaël Caruso" }]
keywords = ["spotify", "playlist", "music", "genre", "cli"]
dependencies = [
    "spotipy>=2.23",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=7", "pytest-cov>=4", "ruff>=0.6", "responses>=0.25"]

[project.urls]
Homepage = "https://github.com/arlq/spotify-playlist-sorter"

[project.scripts]
spotify-sorter = "spotify_sorter.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
spotify_sorter = ["../config/*.yaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=spotify_sorter --cov-report=term-missing --cov-report=xml"

[tool.coverage.run]
source = ["spotify_sorter"]
branch = true

[tool.coverage.report]
show_missing = true
# Regression floor. The network layer is tested against a `responses`-backed mock
# of the Spotify Web API (see tests/spotify_api.py), so coverage is high. Keep this
# just below the current level; raise it as coverage grows — never lower it.
fail_under = 97

[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
extend-select = ["I", "UP", "B"]  # import-sort, pyupgrade, bugbear
